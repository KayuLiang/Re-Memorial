# 生命值实现进度

2026-09-21。活动工程：`E:\RenpyProject\ReMemorial`。本轮未提交或推送 Git。

## 已接入

- 上限为正式体质 × 5，不计属性值、临时加成或疾病临时减值；新角色满生命。
- 正式体质每个实际增减点，同时使当前生命和上限增减 5，保留已受伤的点数；当前生命归零即死亡。
- 夜间睡眠的小休结算后恢复上限的 2.5%，保留小数、不溢出；包含熬夜及昏迷后的夜间睡眠。起床不重复恢复，日间休息不回血。
- 普通治疗或体质成长不能复活已死亡角色。
- 病期到期永久扣体质造成死亡时，睡眠/起床流程停止推进，显示读取存档或返回主菜单；不删除存档。
- HUD 既有心形圆环显示剩余比例，悬停或点击显示精确数值；位置、心形图标及魔力占位不变。
- 事件伤害、医院治疗调用 `rm_damage_health(amount)`、`rm_heal_health(amount)`；具体数额、费用和日程仍待设计，不提供临时免费治疗入口。

## 存档与回滚

- 旧角色缺少生命值时仅初始化一次为当时的生命上限；已有生命值不重置。
- 实测发现普通 Python 角色对象读档会重放伤害语句，导致重复扣血。改用 Ren'Py 原生 RevertableObject，并使正式属性字典参与回滚；旧字典在状态初始化时转换。独立 Python 测试使用普通 object/dict。
- 已验证保存 15.75 → 致死 → 读取恢复 15.75，以及体质下降后回滚同时恢复体质、当前生命和上限。
- 2026-09-21 后续数值审计已将角色的其他嵌套数值容器和骰子对象接入 Ren'Py 原生回滚；详情见 `numeric-save-rollback-status.md`。

## 验证与手册

- 全部 208 项 Python 单元测试通过，其中生命值 11 项。
- Ren'Py 8.5.3 `test health`：4 用例、22 断言通过，覆盖 HUD、读档、回滚、睡眠和起床致死。
- `test sleep_cycle`：3 用例、46 断言通过。
- lint 通过，仅保留原有 im.Scale 弃用提醒。
- 已查看满生命、半生命、0.125 生命和死亡画面；圆环空白部分不再残留满圈白边。
- 在 `docs/manuals/数值系统手册_检定算法同步_20260920.docx` 局部补充小休恢复说明、第 8 章生命值规则及目录，保留原样式。Word 导出 54 页并逐页检查；配套渲染器缺少 LibreOffice，采用现有 Word 导出工具。
- 运行证据与文档预览位于 `E:\ChatGPT\Temp\rememorial-health`。测试截图仅用于人工检查，不作为像素基准门禁。
