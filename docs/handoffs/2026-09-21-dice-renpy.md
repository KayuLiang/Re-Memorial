# 骰组接入 Ren'Py

## 2026-09-22 已确认卡面接入

- 用户明确通过的图片是 `exec-36983e98-62d0-4b0b-8d5f-58e58c52de0c.png`，来自本任务 `E:/ChatGPT/CodexHome/generated_images/01a0b8fd-2334-7201-9bc6-c7493c00d4c8/`。
- 原图直接复制为 `game/gui/dice_obsidian/card-stock-approved.png`，未重画、未加雾或噪点。卡面是黑底金框、断续几何线和正圆＋水平星环。
- 陈列与展开底牌共用 `rm_dice_card_stock()`；图片按原始 2:3 比例显示为 340×510，居中于原 360×510 点击区域，避免拉伸中央圆形。展开时整张卡片沿既有 Blender 投影倾斜，图案本身没有额外椭圆星环。
- 选中框改为贴合新卡面的一圈浅金轮廓；标题内移，月相横向半径从 132 收至 120，避免压到边框。
- 未改骰子模型、数字材质、面值、成长规则或动画时序；保留第一次选中、第二次展开、旋转落定、右下拱形入场、D20 连续滚动。
- 旧 `card-stock.svg` 留在磁盘但不再被骰组界面引用。未提交或推送 Git。
- 验证：最终修改后原生骰组测试在 1280×720、1920×1080、2560×1440 全部通过（6 用例、123 断言），日志 `tmp/dice_collection/card-approved-test.log`；已查看陈列、展开和入场中途的实机截图。Lint 通过，仅保留原有 `im.Scale` 提示。测试使用独立存档目录，未改玩家存档。

## 本次交付

实际项目：`E:/RenpyProject/ReMemorial`。HUD 原有骰组入口现在进入原生陈列界面，不再打开旧文字骰子列表。病例其他页面和投骰/成长规则未修改。

- 陈列、展开、成长三种视图沿用已确认的黑白版式、黑曜石骰体和金色数字。
- 面值分布常驻右侧，不需要点击。横轴是点数，纵轴是对应物理面数量，包括零面数的档位；这不是附魔之后的最终投掷概率。
- 首次点卡片只选中，再点同一卡片展开；显式展开按钮也可展开。
- 小骰保留所有物理面；D8/D10/D12 展开后全数可见；D20 可滚轮、左右键、Home/End 或箭头浏览，选中面保持可见。
- 卡片 D4/D6 展示所有面，较高面数展示五个实际面与省略号。重复值不会合并物理面，不使用 x 次下标。
- 成长只在成长页显示，读取实际 `dice_growth_progress` 和待选择奖励，不提供结算/领取/修改操作。
- 月相使用原创 SVG 和固定曲线，不使用生成图；不存在直线二等分。它只是视觉表现，不添加月相规则。
- 术语及排列为力量、灵巧、体质、智力、意志。仅骰组展示层覆盖历史术语，不改核心规则字段。

## 数据与素材

- `game/systems/rm_dice_view.py`：纯只读整理，实时读取 faces、附魔、序号、范围、均值和分布。
- `game/ui/rm_dice_collection.rpy`：界面与动效。Blender 骰体上的数字也由实时 faces 投影绘制，不会因成长变值而保留旧烘焙数字。
- `game/gui/dice_obsidian/`：D4/D6/D8/D10/D12/D20 各 36 个旋转落定帧、数字与纸牌投影数据、月相 SVG、Almendra 字体及 OFL 许可。
- `art_exports/D20_SAMPLE_20260920/obsidian_models/`：六种面数的可编辑 Blender 模型。
- `art_exports/D20_SAMPLE_20260920/build_obsidian_ui.py`：从先前认可的矿物源材质制作黑曜石，导出空白骰体和逐面投影。未覆盖旧蓝色源工程。
- `tools/build_dice_moons.py`：重建月相资源。

数字的金色来自原生文字颜色/高光描边；骰体是 Blender 预渲染，不在 Ren'Py 中运行实时三维引擎。旋转采用 36 帧、1.2 秒的慢—快—慢序列，末尾有小幅回正。关闭 Ren'Py 的过渡效果时直接显示静态布局。

## 动效修复（同日后续）

原先的静态截图及按钮测试没有验证动效完整性，不能作为入场/滚动效果已完成的证据。原版纸牌为手画四边形，展开直接切换页面，滚轮直接替换索引。

- 纸牌四角现在来自骰子同一 Blender 相机对水平平面的投影；缩放与骰体共用 `RM_DICE_STAGE`，不再独立猜透视。`--pages-only` 可只重建投影。
- 再点选中卡片或点击“展开骰面”，都从该卡片实际所在列/行起步。纸牌倾斜下放，骰子连续移动、放大、旋转落定。
- 每次展开有独立的入场时间；点击骰面查看数值不会重播，退出再进会重播。
- 月相保持间距，从右下沿同一拱形进入。D20 滚轮、左右键、箭头及 Home/End 通过连续角度插值移动；中途反向从当时的位置接续。D4/6/8/10/12 保持全显示。
- 仍然只读：没有修改骰面、附魔、成长或存档规则；没有增加粒子、发光法阵或新菜单。
- 原生运动测试在 `game/tests/dice_collection.rpy`，覆盖重入、二排卡片原点、实际滚轮及反向、首尾面选择，并在入场中途截图。设 `RM_DICE_RECORD=1` 时额外录制 1280×720 游戏窗口，输出 `tmp/dice_collection/dice-motion.mp4`；录制是可选的本机工具，不是游戏依赖。
- 保存中的界面若在系统重启后恢复，过期的单调时钟时间按落定画面处理，避免动画永久停在起点。旧展开页新增变量的默认值继承已有的滚动位置。
- 修复后验证：完整骰组 6 个用例 / 120 个断言通过；最后的兼容性小修正后，动效专项在三种分辨率再次通过（3 个用例 / 33 个断言）。病例回归 108 个断言通过，Python 210 项通过，compile / lint 通过（只有已有 im.Scale 提示）。最终记录分别为 `motion-test-final.log`、`motion-only-final.log`、`motion-case-regression.log`。
- `dice-motion.mp4` 是游戏窗口实际录制的 20 秒 / 600 帧视频，不是另做的动画演示。入场与滚动还抽帧检查了间距、纸牌/骰体位置和数字可读性。
- HUD 回归最终通过 156 个断言（`motion-hud-regression.log`）。测试游戏进程已关闭，未修改正常玩家存档，未提交或推送。

## 验证

- Ren'Py compile / lint 通过；lint 仅列出原项目 `game/images.rpy` 的旧式 im.Scale 提示。
- Python：210 项测试通过。
- 原生骰组：90 个断言通过，精确窗口 1280×720、1920×1080、2560×1440；覆盖六种面数、重复点数、零档分布、两次点击、筛选空态、排序、分页、成长跳转、键盘与 Esc、关闭动效、只读检查。
- 病例原生回归：108 个断言通过（含存档、回滚和状态档案）。
- HUD 原生回归：156 个断言通过。
- 原生截图位于 `tmp/dice_collection/<分辨率>/`，为明确标记的测试数据，不是修改过的玩家存档。

Windows 高 DPI 会改变窗口实际尺寸。运行上述精确尺寸测试时，测试进程设置 `RENPY_HIGHDPI=1`；不修改系统显示设置。存档使用独立 `--savedir`，骰组测试明确关闭自动存档。

```powershell
$env:RENPY_HIGHDPI='1'
& 'E:/renpy-8.5.3-sdk/lib/py3-windows-x86_64/python.exe' 'E:/renpy-8.5.3-sdk/renpy.py' 'E:/RenpyProject/ReMemorial' test dice_collection --overwrite-screenshots --savedir 'E:/RenpyProject/ReMemorial/tmp/dice_collection/saves'
```

未改动背包、手机、药盒，也未提交或推送 Git。

## D10 比例调整

按用户反馈将 D10 模型 X/Y 方向加宽 40%，Z 高度不变，保留十个四边形骰面、尖端位置及黑曜石材质。仅重建 D10 的 36 帧、数字投影和 Blender 模型，未调整其他骰子或动效时序。三个分辨率的陈列/展开回归通过 90 个断言（`tmp/dice_collection/d10-width-test.log`）；更新截图为 `tmp/dice_collection/1920x1080/d10.png`，调整前截图保留在 `tmp/dice_collection/d10-before-width.png`。

随后按“放在同一个球体里，腰部最宽”的反馈，将固定加宽系数替换为同球面内接约束：全部 12 个顶点半径 1.65，总高 3.30、腰部直径约 3.282，仍保留十个平整四边形骰面。球体仅作比例约束，不渲染外壳。D10 的 36 帧与数字投影再次同步；三分辨率回归 90 个断言通过（`d10-sphere-test.log`）。最终预览 `tmp/dice_collection/d10-spherical-preview.png`，上一版截图 `d10-before-sphere.png`。

球面版视觉偏宽后，按用户指定改为腰部直径/总高 = 85%：总高 3.30、腰径 2.805。不再要求全部顶点共球面，保持镜头、尖端、材质、动画时序不变。模型检查通过：十个平整四边形面，实测比例 0.84999998；36 帧与数字投影同步重渲。三分辨率回归通过 90 个断言（`d10-85-test.log`），预览 `tmp/dice_collection/d10-85-preview.png`，等待用户判断此比例。
